import React, { ReactElement } from 'react'

interface Props {
    
}

function Navbar({}: Props): ReactElement {
    return (
        <div>
            Navbar
        </div>
    )
}

export default Navbar
